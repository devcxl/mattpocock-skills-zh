# 致谢

"the shape of the change" 一节来自 [Dex Horthy](https://github.com/dexhorthy) 的 [`show-me`](https://github.com/humanlayer/humanlayer) 技能，几乎逐字复刻，只是把目标从实时对话换成了 diff。`pr` 并不把 `show-me` 当作技能依赖（它不属于本仓库，硬依赖会破坏独立安装），所以内容是被复制进来的，而不是指向外部；本文件就是这种依赖原本会承载的署名。
